import { input, select, confim } from '@inquirer/prompts';

const users = [];


/* func1 add users

for ( ; is Adding === true; )
const name = await imput ({message: 'Enter user name (or press ENTER to finish):',});
if (name.trim() === ''){
   console.log('Finish registration.');
   break;
}
const gender = await select({message: 'Select gender for ${name}:',
  choices: [
  {name: 'Male', value: 'male'},
  {name: 'Female', value: 'female'},
  ],  
});
const age = await imput({message: 'Enter age for ${name}:',
});
users.push({
      name: name.trim(),
      gender: gender,
      age: parseInt(age, 10),
    });
*/
